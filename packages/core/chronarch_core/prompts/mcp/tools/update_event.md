Edit event fields. Time changes additionally require reschedule permission. WRITE-tier: callers should preview material changes. Naive `start`/`end` resolve in `timezone` (IANA), else the credential owner's home zone, else UTC. scope picks series (default), this (one occurrence), or future (split the series at the cut); scoped edits target instance_start, defaulting to the next upcoming occurrence. CalDAV supports series only.
